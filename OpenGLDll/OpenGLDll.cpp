#include "stdafx.h"

bool initialize(ErrorType &error)
{
	std::cout << "Lib!" << std::endl;

	//viewer = new Viewer();
	//camera = new Camera();

	//viewer->setCamera(camera);


	//viewer->initialize();



	return true;
}